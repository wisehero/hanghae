package chap02.passwordchecker

import org.junit.jupiter.api.Test

internal class PasswordStrengthMeterTest {

    // 암호 규칙에 따른 강도는 다음과 같다.
    // 1. 길이가 8글자 이상
    // 2. 0부터 9까지 숫자를 포함
    // 3. 대문자를 포함

    @Test
    fun `세 가지 규칙을 전부 포함하면 암호 세기는 강함이다`() {

    }
}