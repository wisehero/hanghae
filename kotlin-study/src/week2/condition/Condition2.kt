package week2.condition

fun main() {
    var eventName = "참새"
    var myName = "참새"

    if (myName == eventName) {
        println("환영합니다 ${myName}님! 이벤트에 당첨되었어요!")
    } else {
        println("환영합니다 ${myName}님!")
    }
}